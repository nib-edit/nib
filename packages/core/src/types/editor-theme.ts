import { KeyValue } from './common';

export interface EditorTheme {
  color: KeyValue;
  fontSize: KeyValue;
  fontWeight: KeyValue;
  borderRadius: KeyValue;
  border: KeyValue;
  boxShadow: KeyValue;
}
