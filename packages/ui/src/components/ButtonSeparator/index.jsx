import React from "react";
import styled from "@emotion/styled";

export default props => <ButtonSeparator {...props} />;

export const ButtonSeparator = styled.div`
  display: inline-block;
  width: 4px;
`;
