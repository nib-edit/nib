import { nodes } from 'nib-schema';

const {
  table,
  table_row: tableRow,
  table_cell: tableCell,
  table_header: tableHeader,
} = nodes;

export default {
  nodes: {
    table,
    table_row: tableRow,
    table_cell: tableCell,
    table_header: tableHeader,
  },
};
